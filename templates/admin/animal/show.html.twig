{% extends 'base.html.twig' %}

{% block title %}Animal{% endblock %}

{% block body %}
    <h1>Animal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ animal.prenom }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ animal.etat }}</td>
            <tr>
                <th>Race</th><td>{{ races}}</td>
                {% for race in races %}
                    <td>{{ race.id }}</td>
                {% endfor %}
            </tr>
            <tr>
                <th>Image des animal</th>
                <table class="table"><tbody>
                {% for animalImage in animal.animalImages %}
                    <tr><td>{{ animalImage.getImageData() }}</td></tr>
                {% endfor %}
                </table></tbody>
            </tr>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_animal_index') }}">Retour à la listet</a>

    <a href="{{ path('app_admin_animal_edit', {'id': animal.id}) }}">Modifier</a>

    {{ include('admin/animal/_delete_form.html.twig') }}
{% endblock %}
