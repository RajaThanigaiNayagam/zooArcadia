{% extends 'base.html.twig' %}

{% block title %}Race{% endblock %}

{% block body %}
    <h1>Race</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ race.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ race.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_race_index') }}">back to list</a>

    <a href="{{ path('app_admin_race_edit', {'id': race.id}) }}">edit</a>

    {{ include('admin/race/_delete_form.html.twig') }}
{% endblock %}
