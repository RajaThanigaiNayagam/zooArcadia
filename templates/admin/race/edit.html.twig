{% extends 'base.html.twig' %}

{% block title %}Modifier la race de l'animal{% endblock %}

{% block body %}
    <h1>Modifier la race de l'animal</h1>

    <div class="datatable">
        {{ include('admin/race/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    
    <a href="{{ path('app_admin_race_index') }}"><button class="btn btn-outline-light my-3">Retour à la liste</button></a>

    {{ include('admin/race/_delete_form.html.twig') }}
{% endblock %}
