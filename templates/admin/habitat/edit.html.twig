{% extends 'base.html.twig' %}

{% block title %}Modifier l'habitat{% endblock %}

{% block body %}
    <h1>Modifié l'habitat</h1>

    <div class="datatable">
        {{ include('admin/habitat/_form.html.twig', {'button_label': 'Update'}) }}
    
        <a href="{{ path('app_admin_habitat_index') }}"><button class="btn btn-outline-success my-3">Retour à la liste</button></a>

        {{ include('admin/habitat/_delete_form.html.twig') }}
    </div>
{% endblock %}
