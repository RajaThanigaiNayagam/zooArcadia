<form method="post" action="{{ path('app_admin_horaire_delete', {'id': horaire.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ horaire.id) }}">
    <button class="btn"><button class="btn btn-outline-success my-3">Supprimer</button></button>
</form>
