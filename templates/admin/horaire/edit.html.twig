{% extends 'base.html.twig' %}

{% block title %}Edit Horaire{% endblock %}

{% block body %}
    <h1>Edit Horaire</h1>

    <div class="datatable">
        {{ include('admin/horaire/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <a href="{{ path('app_admin_horaire_index') }}"><button class="btn btn-outline-light">Retour à la liste</button></a>

    {{ include('admin/horaire/_delete_form.html.twig') }}
{% endblock %}
