{% extends 'base.html.twig' %}

{% block title %}Images de l'habitat{% endblock %}

{% block body %}
    <h1>Images de l'habitat</h1>

    <div class="table-responsive datatable">
        <table class="table">
        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ image.id }}</td>
                </tr>
                <tr>
                    <th>Image_data</th>
                    <td>{{ image.imageData }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_admin_habitat_image_index') }}"><button class="btn btn-outline-success my-3">Retour à la liste</button></a>

        <a href="{{ path('app_admin_habitat_image_edit', {'id': image.id}) }}"><button class="btn btn-outline-success my-3">Modifier</button></a>

        {{ include('admin/habitat_image/_delete_form.html.twig') }}
    </div>
{% endblock %}
