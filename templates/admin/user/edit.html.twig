{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    <div class="datatable">
        {{ include('admin/user/_form.html.twig', {'button_label': 'Update'}) }}
    
        <a href="{{ path('app_admin_user_index') }}"><button class="btn btn-outline-success my-2">Retour à la liste</button></a>

        {{ include('admin/user/_delete_form.html.twig') }}
    </div>
{% endblock %}
