<form method="post" action="{{ path('app_admin_animal_image_delete', {'id': animal_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animal_image.id) }}">
    <button class="btn bet-outline-dark">Supprimer</button>
</form>
