{% extends 'base.html.twig' %}

{% block title %}AnimalImage{% endblock %}

{% block body %}
    <h1>AnimalImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal_image.id }}</td>
            </tr>
            <tr>
                <th>Image_data</th>
                <td>{{ animal_image.imageData }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_animal_image_index') }}">back to list</a>

    <a href="{{ path('app_admin_animal_image_edit', {'id': animal_image.id}) }}">edit</a>

    {{ include('admin/animal_image/_delete_form.html.twig') }}
{% endblock %}
