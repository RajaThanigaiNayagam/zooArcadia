{% extends 'base.html.twig' %}

{% block title %}AnimalImage{% endblock %}

{% block body %}
    <h1>AnimalImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal_image.id }}</td>
            </tr>
            <tr>
                <th>Image_data</th>
                <td>{{ animal_image.imageData }}</td>
            </tr>
        </tbody>
    </table>

    <img src="{{ asset( animal_image.imageData ) }}" alt="" class="img-rounded" width ="200" height="300" tyle="">

    <a href="{{ path('app_admin_animal_image_index') }}">Retour à la liste</a>

    <a href="{{ path('app_admin_animal_image_edit', {'id': animal_image.id}) }}">Modifier</a>

    {{ include('admin/animal_image/_delete_form.html.twig') }}
{% endblock %}
