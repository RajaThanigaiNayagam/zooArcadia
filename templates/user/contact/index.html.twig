{% extends 'base.html.twig' %}

{% block title %}Page de contace{% endblock %}

{% block body %}
    <h1>Page de contact</h1>
    
    <div class="datatable">
        <h4>Vous pouvez envoyer votre message à l'adresse email - contact@zoo.fr</h4>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-outline-success my-3">{{ button_label|default('Soumettre') }}</button>
        {{ form_end(form) }}
    </div>

    
{% endblock %}
