{% extends 'base.html.twig' %}

{% block title %}RapportEmployee{% endblock %}

{% block body %}
    <h1>RapportEmployee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rapport_employee.id }}</td>
            </tr>
            <tr>
                <th>Nourriture</th>
                <td>{{ rapport_employee.nourriture }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ rapport_employee.createdAt ? rapport_employee.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Quantité</th>
                <td>{{ rapport_employee.quantité }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_veterinaire_rapport_employee_index') }}">back to list</a>

    <a href="{{ path('app_veterinaire_rapport_employee_edit', {'id': rapport_employee.id}) }}">edit</a>

    {{ include('veterinaire/rapport_employee/_delete_form.html.twig') }}
{% endblock %}
