{% extends 'base.html.twig' %}

{% block title %}Animal{% endblock %}

{% block body %}
    <h1>Animal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ animal.prenom }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ animal.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_veterinaire_animal_index') }}">back to list</a>

    <a href="{{ path('app_veterinaire_animal_edit', {'id': animal.id}) }}">edit</a>

    {{ include('veterinaire/animal/_delete_form.html.twig') }}
{% endblock %}
