{% extends 'base.html.twig' %}

{% block title %}Animal{% endblock %}

{% block body %}
    <h1>Animal</h1>

    <div class="table-responsive datatable">
        <table class="table table-bordered border-success">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ animal.id }}</td>
                </tr>
                <tr>
                    <th>Prenom</th>
                    <td>{{ animal.prenom }}</td>
                </tr>
                <tr>
                    <th>Etat</th>
                    <td>{{ animal.etat }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_veterinaire_animal_index') }}"><button class="btn btn-outline-success my-3">Retour à la liste</button></a>

        <a href="{{ path('app_veterinaire_animal_edit', {'id': animal.id}) }}"><button class="btn btn-outline-success my-3">Modifier</button></a>

        {{ include('veterinaire/animal/_delete_form.html.twig') }}
    </div>
{% endblock %}
