<form method="post" action="{{ path('app_employee_rapport_delete', {'id': rapport_employee.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport_employee.id) }}">
    <button class="btn">Supprimer</button>
</form>
