{% extends 'base.html.twig' %}

{% block title %}Edit RapportEmployee{% endblock %}

{% block body %}
    <h1>Edit RapportEmployee</h1>

    <div class="datatable">
        <div class="avis_section"><h6>Date et heure actuelles - {{ "now"|date("m/d/Y - H:i") }}</h6></div>
        {{ include('employee/rapport/_form.html.twig', {'button_label': 'Update'}) }}
    
        <a href="{{ path('app_employee_rapport_index') }}"><button class="btn btn-outline-success my-2">Retour à la liste</button></a>

        {{ include('employee/rapport/_delete_form.html.twig') }}
    </div>
{% endblock %}
