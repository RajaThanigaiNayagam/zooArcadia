{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avi.id }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ avi.pseudo }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ avi.commentaire }}</td>
            </tr>
            <tr>
                <th>IsVisible</th>
                <td>{{ avi.isVisible ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_employee_avis_index') }}"><button class="btn btn-outline-dark my-3">Retour à la liste</button></a>

    <a href="{{ path('app_employee_avis_edit', {'id': avi.id}) }}"><button class="btn btn-outline-dark my-3">Modifier</button></a>

    {{ include('employee/avis/_delete_form.html.twig') }}
{% endblock %}
